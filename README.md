## requirements
 
 Pour creer l'environnement python avec les dépendances utilisés (requirements.txt):

```
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```


## lib/
--> les scripts et outils de dev

## data/
--> la data